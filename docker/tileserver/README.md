# Tileserver

Coloca aqui el extracto `.mbtiles` requerido para desarrollo local, por ejemplo:

```text
docker/tileserver/loja.mbtiles
```

El entregable base no versiona archivos `.mbtiles` ni descarga mapas. La integracion real de mapas pertenece a un entregable posterior.

